# Backend API configuration (IP and port of the backend service)
# Change this to the full IP and port of the machine running the backend service.
# e.g., VITE_API_URL=http://192.168.1.50:3001
VITE_API_URL=http://localhost:3001
